# Repository release v2

Repository-only reproducibility hardening. No scientific endpoint, statistic, null model, cohort, result, or manuscript claim was changed.

Changes from repository package v1:

- added MIT license;
- added explicit pre-/post-unblinding provenance timeline;
- added exact SHA-256 hashes for frozen code and protocols;
- added top-level SHA256SUMS;
- documented measured runtime for two 4,999-surrogate per-genome runs;
- added a post-unblinding per-genome execution wrapper that preserves the frozen seed schedule and invokes the unchanged frozen runner;
- updated README, reproduction instructions, CFF and Zenodo metadata template.
